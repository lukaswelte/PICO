var ShowEntry = React.createClass ({
    mixins: [FluxMixin],

    render: function () {
        return (
            <div>
                Das ist die Show Entry Seite.
            </div>
        );
    }


});